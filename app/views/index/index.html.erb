<%= line_chart @notes.sample(5).map { |note|
    { name: note.name, data: note.group_values_by_date }
}, width: "100%", height: "100%" %>

<br />
<% @notes.each do |note| %>
  <%= link_to note.name, index_path(note) %>
<% end %>
